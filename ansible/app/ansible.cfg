[defaults]
inventory = inventory.ini
host_key_checking = False
remote_user = azureuser
private_key_file = ~/.ssh/azure_ansible_key

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
